</html>
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- 뷰포트 설정으로 반응형 웹 디자인을 지원합니다. -->
    <title>로그인 페이지</title> 
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }


        body {
            font-family: Arial, sans-serif; /* 기본 글꼴 설정 */
            display: flex; /* 플렉스 박스 레이아웃 사용 */
            justify-content: center; /* 수평 중앙 정렬 */
            align-items: center; /* 수직 중앙 정렬 */
            height: 100vh; /* 전체 화면 높이 설정 */
            background-color: #f0f0f0; /* 배경색 설정 */
        }


        .login-container {
            background-color: white; /* 배경색 설정 */
            padding: 20px; /* 내부 여백 설정 */
            border-radius: 8px; /* 모서리 반경 설정 */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* 그림자 설정 */
            width: 300px; /* 너비 설정 */
            text-align: center; /* 텍스트 중앙 정렬 */
        }


        h2 {
            margin-bottom: 20px; /* 아래쪽 여백 설정 */
        }


        .input-group {
            margin-bottom: 15px; /* 아래쪽 여백 설정 */
            text-align: left; /* 텍스트 왼쪽 정렬 */
        }


        label {
            display: block; /* 블록 요소로 설정 */
            margin-bottom: 5px; /* 아래쪽 여백 설정 */
            font-weight: bold; /* 굵은 글씨체 설정 */
        }


        input {
            width: 100%; /* 너비 100% 설정 */
            padding: 10px; /* 내부 여백 설정 */
            border: 1px solid #ccc; /* 테두리 설정 */
            border-radius: 4px; /* 모서리 반경 설정 */
        }


        button {
            width: 100%; /* 너비 100% 설정 */
            padding: 10px; /* 내부 여백 설정 */
            background-color: #007BFF; /* 배경색 설정 */
            border: none; /* 테두리 제거 */
            border-radius: 4px; /* 모서리 반경 설정 */
            color: white; /* 텍스트 색상 설정 */
            font-size: 16px; /* 글씨 크기 설정 */
            cursor: pointer; /* 커서 모양 변경 */
        }


        button:hover {
            background-color: #0056b3; /* 배경색 변경 */
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Login</h2> 
        <form action="/login" method="POST">
            <div class="input-group">
                <label for="username">사용자 이름</label> 
                <input type="text" name="id" required>
            </div>
            <div class="input-group">
                <label for="password">비밀번호</label> 
                <input type="password" name="password" required>
            </div>
            <button type="submit">확인</button> 
            <button onclick="window.location.href='/signup'">회원가입</button>
        </form>
    </div>
</body>
</html>
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원가입</title>
</head>
<style>
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }


    /* 페이지 기본 스타일 설정 */
    body {
        font-family: Arial, sans-serif; /* 기본 글꼴 설정 */
        display: flex; /* 플렉스 박스 레이아웃 사용 */
        justify-content: center; /* 수평 중앙 정렬 */
        align-items: center; /* 수직 중앙 정렬 */
        height: 100vh; /* 전체 화면 높이 설정 */
        background-color: #f0f0f0; /* 배경색 설정 */
    }


    /* 폼 컨테이너 스타일 설정 */
    .form-container {
        background-color: white; /* 배경색 설정 */
        padding: 20px; /* 내부 여백 설정 */
        border-radius: 8px; /* 모서리 반경 설정 */
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* 그림자 설정 */
        width: 300px; /* 너비 설정 */
        text-align: center; /* 텍스트 중앙 정렬 */
    }


    /* 제목 스타일 설정 */
    .form-container p {
        margin-bottom: 20px; /* 아래쪽 여백 설정 */
        font-size: 18px; /* 글씨 크기 설정 */
        font-weight: bold; /* 글씨 두께 설정 */
    }


    /* 입력 필드 스타일 설정 */
    input[type="text"], input[type="password"] {
        width: 100%; /* 너비 100% 설정 */
        padding: 10px; /* 내부 여백 설정 */
        margin: 10px 0; /* 위아래 마진 설정 */
        border: 1px solid #ccc; /* 테두리 설정 */
        border-radius: 4px; /* 모서리 반경 설정 */
    }


    /* 제출 버튼 스타일 설정 */
    input[type="submit"] {
        width: 100%; /* 너비 100% 설정 */
        padding: 10px; /* 내부 여백 설정 */
        background-color: #007BFF; /* 배경색 설정 */
        border: none; /* 테두리 제거 */
        border-radius: 4px; /* 모서리 반경 설정 */
        color: white; /* 글자 색상 설정 */
        font-size: 16px; /* 글씨 크기 설정 */
        cursor: pointer; /* 커서 모양 변경 */
    }


    /* 제출 버튼 호버 효과 설정 */
    input[type="submit"]:hover {
        background-color: #0056b3; /* 배경색 변경 */
    }
</style>
<body>
    <div class="form-container">
        <p>회원가입 창</p> 
        <form action="/signup" method="POST">
            <input type="text" name="id" placeholder="아이디" required><br />
            <input type="password" name="password" placeholder="비밀번호" required><br />
            <input type="text" name="name" placeholder="이름" required><br />
            <input type="text" name="resident_number" placeholder="주민등록번호" required><br />
            <input type="text" name="phone_number" placeholder="전화번호" required><br />
            <input type="text" name="date" placeholder="생년월일"><br />
            <label for="favorite">좋아하는 구단:</label><br>
            <select id="favorite_team" name="favorite_team">
                <option value="KIA">KIA</option>
                <option value="LG">LG</option>
                <option value="두산">두산</option>
                <option value="삼성">삼성</option>
                <option value="SSG">SSG</option>
                <option value="NC">NC</option>
                <option value="한화">한화</option>
                <option value="롯데">롯데</option>
                <option value="KT">KT</option>
                <option value="키움">키움</option>
            </select><br><br>

            <input type="submit" value="제출">
        </form>
    </div>
</body>
</html>